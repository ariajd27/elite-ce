#ifndef trig_include_file
#define trig_include_file

signed int trig_cos(unsigned char const angle); // 64 for a full circle
signed int trig_sin(unsigned char const angle); // 64 for a full circle

#endif
